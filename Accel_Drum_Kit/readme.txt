#Accel_Drum_Kit
Uses a MPU6050 accelerometer part to trigger drum hits via UART.
Used originally in an Arduino <-> Raspberry Pi setup, where the Pi receives message via UART and plays wav files via pygame (for low-latency) accordingly.